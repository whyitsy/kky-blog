# P0 执行记录

> 对应计划：[`./2026-09-14-实施计划-P0清尾.md`](./2026-09-14-实施计划-P0清尾.md)
> 分支：`chore/p0-cleanup`（从 `origin/main @ 6024563` 切出）
> 开始：2026-09-14

---

## Task 0 · 测试基线 ✅

| 项 | 结果 |
|---|---|
| 命令 | `dotnet test Blog.Backend/Blog.Backend.slnx --configuration Release` |
| 结果 | **失败 0，通过 195，跳过 0，总计 195，耗时 8 s** |
| 环境 | Windows dotnet SDK **10.0.401**（WSL 内经 `/mnt/c/Program Files/dotnet/dotnet.exe` 调用） |
| 数据库 | 开发容器 `pgsql`（`blog_stage2`，端口 5432），当时 **3 篇文章** |

**意义**：文档里的"195 项测试"此前只有**静态计数**佐证（73 Fact + 71 InlineData + 51 MemberData），
本次是**运行实测**，数字吻合。同时它纠正了散落在 `docs/09` §5.7、`docs/10` §6、`docs/11` §7.4、
`简历/01` 里的旧值 **81**。

**顺带发现（要带进 P1 文档重构）**：

1. `docs/12` §8.2 记录"开发库数据量：Posts **13**"，实测开发库 `blog_stage2` 只有 **3** 篇。
   这正是方案 §3.3 "不写死会变的数字"要解决的问题——这类快照数字必然漂移。
2. 客户端时间与真实时间无关，但 `git log` 的提交时间是 2026-09-13/14，与文档的"最后核对"日期一致。

---

## Task 1 · 统一响应体缺口 ✅

| 项 | 结果 |
|---|---|
| 改动 | `Blog.WebApi/Program.cs`（+37 行：`ApiBehaviorOptions.InvalidModelStateResponseFactory`）<br>`Blog.Tests/Integration/UnifiedErrorResponseTests.cs`（新增 84 行 / 3 个用例） |
| TDD 红 | 3 个用例 → **失败 2、通过 1**，失败信息正是 ProblemDetails：<br>`缺少 code 字段：{"type":"https://tools.ietf.org/html/rfc9110#section-15.5.1","title":"One or more validation errors occurred.","status":400,"errors":{"keyword":["The keyword field is required."]},"traceId":"…"}` |
| TDD 绿 | 3 个用例全通过 |
| 端到端实测 | `curl localhost:8080/api/posts/search` → `{"code":4001,"message":"参数「keyword」不合法或缺失","data":null}` ✅<br>正常检索 → `{"code":0,…}` ✅ ／ 列表 200 ✅ ／ `/health` 200 ✅ |

**设计要点**：请求体级错误（模型状态键为 `""` 或 `"$"`）与参数级错误分开措辞——
前者说"请求体格式不正确"，后者说"参数「X」不合法或缺失"。
融合成一句话会在 JSON 语法错误时给出误导性提示（实测第二条用例的键就是 `$`）。

## Task 2 · 窄屏侧栏残留 ✅

| 项 | 结果 |
|---|---|
| 改动 | `Blog.FrontEnd/src/layouts/AdminLayout.vue`（`@media (max-width:880px)` 内 `.admin-side` 补 `position:static; height:auto`） |
| 构建验证 | `npm run build`（含 `vue-tsc -b` 类型检查）→ exit 0，`✓ built in 447ms` |
| 人眼验收 | ⏳ 待你在 <880px 窗口下打开 `/admin/posts` 确认横向 tab 只占一行 |

**根因**：媒体查询只覆盖**写在其中**的属性，**不会撤销**其它规则里的 `position:sticky` / `height:100vh`。

## Task 3 · `.editorconfig` + 格式门禁 ✅

| 项 | 结果 |
|---|---|
| 新增 | `.editorconfig` |
| 改动 | `.github/workflows/ci.yml`（backend 作业新增"检查 C# 格式"步骤）+ 26 个 C# 文件的一次性格式化 |
| 门禁本地复现 | `dotnet format … whitespace --verify-no-changes --no-restore` → **通过** |
| YAML 校验 | 4 个作业；backend 15 步；格式检查位于"还原依赖"之后、"编译"之前 ✅ |

**两条实测得到的经验（都已写进 `.editorconfig` 注释）**：

1. **`end_of_line = lf` 是必须的。** 加之前，`dotnet format` 提议的替换内容全是 `'\r\n'`——
   它会与 `.gitattributes` 和 hygiene 作业**直接冲突**；加了之后 CRLF 提案归零。
2. **只在 whitespace 上设门禁。** `--severity info` 在本仓库报 21 条 analyzer 建议
   （CA2016/CA1873/CA1816/CA1861/SYSLIB1045/ASP0025），一次性纳入属过度工程化。

**对计划的一处偏离**：计划写的是把门禁放进 `hygiene` 作业，实际放进了 `backend` 作业。
理由：hygiene 刻意不装 .NET SDK（5 秒跑完，设计意图是"最快失败"），
装 SDK + restore 会让它失去这个性质。放在 backend 的 restore 之后、
build/test 之前，既复用了还原结果，又仍然**早于**昂贵的测试。

## Task 4 · 备份与恢复脚本 ✅

| 项 | 结果 |
|---|---|
| 新增 | `tools/backup/{backup.sh,restore.sh,README.md}`；`.gitignore` 增加 `backups/` |
| 真实备份 | `blog_blog_stage2_20260914-032545.sql.gz`，**9915 字节** |
| **真实还原演练** | `Posts=3`、`Users=2`、`zhparser 扩展=1`、`chinese 检索配置=1` ✅ |

**⭐ 演练当场推翻了一个错误假设（这是本次最有价值的一条记录）**

我最初在 README 里写的是「zhparser 扩展与 chinese 配置**不在** `pg_dump` 的输出里」。
演练后核实：**错**。dump 第 26 行就是 `CREATE EXTENSION IF NOT EXISTS zhparser WITH SCHEMA public;`，
第 40 行起是 `CREATE TEXT SEARCH CONFIGURATION public.chinese (…)`。

真实风险是另一回事，且已用实验证实：把同一份 dump 灌进一个**普通的 `postgres:18.6`** 实例：

```
ERROR:  extension "zhparser" is not available
HINT:  The extension must first be installed on the system where PostgreSQL is running.
```

**数据在，检索能力不在。** 结论：还原的目标实例必须**预先装好 zhparser 扩展文件**
（它不在官方镜像里）—— 这才是生产必须用带 zhparser 的镜像的真正理由。

> 📌 2026-09-19 更新：当时用的是自编译的 `deploy/postgres-zhparser.Dockerfile`（该文件已删除），
> 现在改用社区镜像 `mixdeve/postgres-zhparser:18` —— 结论不变，只是镜像来源换了。

📌 这条记录本身就是"备份没验证过等于没有备份"的最佳注脚：
**一个听起来合理但错误的假设，只有真跑一遍才会露出来。**

## Task 5 · Dependabot PR ⏳ 待你操作

（见计划 Task 5 的清单）

## Task 6 · 收口

（待填）
