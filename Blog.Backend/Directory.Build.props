<Project>

  <!--
    ════════════════════════════════════════════════════════════════════════
    解决方案级构建属性（放在解决方案根，自动作用于 Blog.Backend/ 下的所有项目）

    为什么需要这个文件：**版本号是「整个应用」的属性，不是某个程序集的属性。**
    之前只写在 Blog.WebApi.csproj 里，而读取它的 BuildInfoProvider 住在
    Blog.Infrastructure —— 于是 GET /api/version 读到的是 Infrastructure 自己的版本，
    静默回退成 "local"。写在这里就不存在"每个项目各配一份、慢慢漂移"的问题。
    ════════════════════════════════════════════════════════════════════════
  -->
  <PropertyGroup>
    <!--
      本地开发的版本号兜底。**它不反映正式发布版本。**

      正式版本的唯一来源是 git tag：CI 在打 tag 发布时用
      build-arg VERSION=(tag) 注入镜像，运行期由 BuildInfoProvider
      优先读环境变量（见 deploy/webapi.Dockerfile 与 .github/workflows/ci.yml）。
      这里的值只在「本地 dotnet run」时可见，作用是让
      GET /api/version 有可读输出，而不是报错或空串。
    -->
    <Version>0.0.0-local</Version>
  </PropertyGroup>

</Project>
