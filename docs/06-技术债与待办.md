# 06 · 技术债与待办

> **用途**：全项目"**现在欠什么、打算做什么、明确不做什么、已废弃什么**"的唯一去处。
> **读者**：准备上线的人、接手项目的人、以及想评估"这个项目能不能用"的人。
> **最后核对**：2026-09-17 ｜ **代码依据**：仓库 HEAD
>
> ⚠️ **本文是全项目唯一允许"过期"的文档。** 其它 `docs/` 只描述当前事实，与代码不符即为缺陷；
> 而本文记的是**尚未发生的动作**——条目执行完就删掉，所以"没跟上"是它的正常状态。
> 每个条目都给出**前置条件**，读者可以自行判断它还成不成立。
>
> 已修复问题的**过程**在 [archive/问题排查记录.md](../archive/问题排查记录.md)（§6 只是索引）；
> 决策的**理由**在 [archive/决策记录.md](../archive/决策记录.md)（§4、§5 是当前口径）；
> 性能与诊断的原理在 [learn/03-性能与运行时诊断.md](../learn/03-性能与运行时诊断.md)，
> 交付链路的原理在 [learn/02-CI-CD实践.md](../learn/02-CI-CD实践.md)。

---

## 1. 应用本身的未决项

| # | 事项 | 类型 | 严重度 | 建议时机 |
|---|---|---|---|---|
| 1 | 前端表单保存链路缺自动化验证 | 验证缺口 | 🟡 中 | 持续 |
| 2 | 没有 API 版本管理 | 演进风险 | 🟢 低 | 需要兼容时 |
| 3 | `FileStorage:Root` 默认值依赖进程工作目录（T19） | 部署健壮性 | 🟢 低 | 上线前 |
| 4 | 前端 TypeScript 停在 6.x（`vue-tsc` 尚不支持 7.x） | 依赖升级阻塞 | 🟢 低 | 等上游 |

> 当前**没有任何 🔴 高优先级事项**。以下四条都不会直接造成线上故障，但会让问题**发现得更晚**
> 或让**演进被迫一刀切**。

### 1.1 前端「点击保存」链路缺自动化验证

后端语义有完整的自动化测试兜底（集成 / 单元 / 统一响应体契约三层；
条数由数据决定，实时值看 CI 作业摘要，**本文不写死**），
乐观锁 409 本身也有集成测试。缺的是**浏览器里的端到端确认**：

| 未自动验证 | 说明 |
|---|---|
| 表单校验提示 | 前端 `@error` 回退、必填校验 |
| 保存成功后的提示与刷新 | toast、列表刷新、`refreshAll()` |
| 乐观锁冲突提示 | 409 时是否给出用户能理解的提示 |
| 骨架屏显示 | 瞬时状态，需要特殊手段才能稳定复现（方法见 [05-运维与部署手册.md](./05-运维与部署手册.md)） |

**已经实测暴露过的一个实例：自动摘要被静默固定。**

| 步骤 | 结果 |
|---|---|
| 新建文章，摘要留空 | ✅ 后端自动截取正文前 50 字 |
| 打开该文章的编辑页 | ⚠️ `PostEditor.syncFromInitial()` 把**自动生成的摘要回填进输入框** |
| 不动摘要直接保存 | ⚠️ 回填文本作为**非空**值提交 → 后端判定为"作者手写的摘要" |
| 之后再修改正文 | ❌ 摘要**不再自动更新**，永久停在第一次保存时的内容 |

**根因**：摘要规则（T4）用「传入是否为空」区分自动 / 手写，但前端**无法区分**输入框里那段文字
是自动生成的还是作者手写的，于是无条件回填、无条件回传。行为与"作者手写的摘要不会被覆盖"
的文档描述不符（不是严重故障，是**行为反直觉**）。

**候选修法**（都需先决策，**均未实施**）：

| 方案 | 做法 | 代价 |
|---|---|---|
| A. 前端不回填 | 编辑页留空，旁边提示"当前摘要（自动生成）：……" | 作者看不见、也改不了现有摘要全文 |
| B. 加 `IsSummaryAuto` 字段 | 显式区分两种状态 | ⚠️ T4 当初已否决（多一个字段、多一类状态不一致） |
| C. 接受现状，只改文档 | 把"一次保存后自动摘要被固定"写成明确行为 | 不改代码，但行为仍反直觉 |
| D. 前端加"恢复自动"按钮 | 保留回填，额外给一个清空 / 恢复按钮 | 新增交互，但语义最清晰且不动后端 |

**验证能力现状**：前端这条链路**没有自动化检查**（浏览器级自动化检查已于 2026-09-19 整体移除），
改动只能靠人眼在浏览器里走一遍；后端行为仍由 `dotnet test` 覆盖。缺的不是工具，是覆盖面。

### 1.2 没有 API 版本管理

所有端点路径**不含版本号**（`/api/posts`，而不是 `/api/v1/posts`）。一旦线上有客户端在运行，
**破坏性变更无法平滑过渡**——只能"改了就全改"，没有新旧并存的余地。

现在可以接受，因为：项目尚未上线；前端与后端**在同一个仓库、同一个提交里演进**，不存在"旧客户端"；
引入版本管理会增加路径噪音。

出现以下任一情况时应该引入：有第三方在调用 API；移动端 App 需要独立发版；
前后端分离部署且发布节奏不同。**建议方案：URL 版本（`/api/v1/...`）**——最直观、最易调试。

### 1.3 `FileStorage:Root` 默认值依赖进程工作目录（T19）

`FileStorage:Root` 的默认值是相对路径 `media`，解析结果**随进程工作目录变化**：
`dotnet run`、`dotnet Blog.WebApi.dll`、测试宿主的工作目录各不相同，上传的文件会散落到意外位置。
生产配置已用绝对路径 `/app/media`（并挂成数据卷），但**默认值本身仍是一个陷阱**——
换一种启动方式就可能踩到。事实描述见 [02-架构与数据模型.md](./02-架构与数据模型.md) §13.2；部署形态见 [05-运维与部署手册.md](./05-运维与部署手册.md)。

**处理时机**：上线前，或引入新的部署形态时。

### 1.4 前端 TypeScript 停在 6.x（`vue-tsc` 尚不支持 7.x）

`typescript` 刻意**不升到 7.x**：v7 是原生（Go）重写版，不再从 `package.json` 导出
`./lib/tsc` 子路径，而 `vue-tsc` 正是靠 `require('typescript/lib/tsc')` 工作的。

**实测证据（2026-09-14，在依赖升级 PR 的分支上）：**

| 步骤 | 结果 |
|---|---|
| 修好 lock（`npm install` 重生成 + `npm ci` 通过） | ✅ 通过 —— 排除"只是 lock 不同步"这个可能 |
| `npm run build`（= `vue-tsc -b && vite build`） | ❌ `Error [ERR_PACKAGE_PATH_NOT_EXPORTED]: Package subpath './lib/tsc' is not defined by "exports" in node_modules/typescript/package.json`<br>栈顶：`at resolveTscPath (node_modules/vue-tsc/index.js:73:43)` |
| `npm view vue-tsc version` | **3.3.11** —— 与仓库里用的版本相同，**没有支持 7.x 的新版本** |

⚠️ **这条为什么必须显式登记、不能靠"等 Dependabot 别再提"**：
`vue-tsc` 的 `peerDependencies` 写的是 `typescript: ">=5.0.0"`，
**7.0.2 名义上满足这个范围** —— 所以 Dependabot 会一直提这个合不了的 PR。
已在 [`.github/dependabot.yml`](../.github/dependabot.yml) 里对 `typescript` 的 major 升级加了 `ignore`
（写法与理由同那里 `SixLabors.ImageSharp` 那条：必须带 `update-types`，否则会连带关掉安全更新通道）。

**解除条件**：`npm view vue-tsc peerDependencies` 明确支持 7.x（或项目改用 `tsgo` 工具链）。
届时删掉那条 ignore 规则即可。

> 📌 附带的教训：`peerDependencies` 的 semver 范围**不等于**真实兼容性。
> 一个大版本重写（如 TS 7 这样的换语言重写）完全可能"范围满足但装上去就崩"——
> 所以**跨大版本的依赖升级必须真跑一次构建**，不能只看它有没有被 peer range 挡住。

---

## 2. 工程化与交付能力缺口（G1~G12）

编号在 2026-09 固定，**后续新增缺口顺延编号**，不重排已有的。

| # | 缺口 | 状态 | 现在欠什么 |
|---|---|---|---|
| **G1** | 没有 CI/CD | ✅ 2026-09-13 | —（流水线 + 分支保护均已落地，Required 检查能真的拦住合并与直推） |
| **G2** | 前端自动化脚本不在仓库里 | ✅ 2026-09-14 | —（有断言的检查曾改写进仓库，并修掉它顺带找出的真实缺陷；2026-09-19 判定不再需要浏览器级自动化，检查与文档已整体移除） |
| **G3** | 没有应用自身的 Dockerfile | ✅ 2026-09-12 | —（两个 Dockerfile + `docker-compose.yml` + `appsettings.Production.json` 已落地并整栈实测） |
| **G4** | 没有发布流程与版本号 | ⚠️ **已落地，未实战 2026-09-19** | 版本号约定、镜像 tag、回滚方案都已落地（`.github/workflows/ci.yml` 的 `publish` / `deploy` + [05-运维与部署手册](./05-运维与部署手册.md) §8.7–§8.9）；⚠️ **还一个 tag 都没推过**，所以这条链路目前只是"写好了"，第一次 `v*` tag 才算验证过 |
| **G5** | 没有依赖安全扫描 | ✅ 2026-09-14 | —（`.github/dependabot.yml` 三生态 + CI 独立作业；只报告不阻断，升级路径见 §3.3） |
| **G6** | 没有 lint / 格式门禁 | ✅ 2026-09-14 | —（`.editorconfig` + `.github/workflows/ci.yml` 的 `dotnet format … whitespace --verify-no-changes`；**只查 whitespace，不查 analyzer**，理由见 §3.3） |
| **G7** | 没有覆盖率度量 | ✅ 2026-09-14 | —（`coverage.runsettings` 排除迁移与生成代码，结果写进 CI 作业摘要，**刻意不设阈值**） |
| **G8** | 没有性能基线与 SLO | ⚠️ **部分完成 2026-09-14** | 基线与 SLO 已落地（`tools/load/` + §3.1 定稿表）；**仍欠"容量"**——目标机器规格未定，SLO 第 8 条只能给口径不给数字 |
| **G9** | 没有 metrics / tracing | ❌ 未做 | 只有 Serilog 文本日志：能回答"发生了什么"，回答不了"现在多慢、趋势如何"。建议时机：有真实流量后 |
| **G10** | 应用侧未用 `ForwardedHeadersMiddleware` | ❌ 未做 | 转发头的信任边界**只写在 nginx 配置里**。当前拓扑（nginx 是唯一入口）下已被 nginx 覆盖；**一旦前面再加 CDN / 云负载均衡、或有人直连 Kestrel，绕过风险就会回来** |
| **G11** | 上传的 SVG 以 `image/svg+xml` 内联下发 | ✅ 2026-09-13 | —（`LocalFileStorageService` 的扩展名与 Content-Type 两张表均移除 `.svg`，直接不支持上传） |
| **G12** | 迁移链无法在干净 PG 上从零建库 | ✅ 2026-09-13 | —（迁移自己先建扩展与检索配置，并移除了测试夹具里的绕过） |

> **仍未关闭的 G8 / G9 / G10 都是"能力缺口"，不是"故障隐患"**：
> 它们不引入漏洞、不损坏数据、不导致服务不可用，但会让**错误发生后的发现与恢复都变慢**。
> 其中 **G8 的剩余部分现在仍能推进**（基线与 SLO 已完成，欠的只有"容量"，
> 而它恰好是**唯一一个前置条件不在项目自己手里**的：目标机器规格）；其余两项各有明确前置：
> G9 等流量、G10 等拓扑变化。
>
> **G4 只差最后一步**：流程与版本号都已写进代码与手册，但**从没推过一个 `v*` tag**。
> 在第一次真发布之前，它只能算"写好了"，不能算"验证过"——这正是本仓库反复强调的
> 「没见过它失败/成功，你无法确信它在工作」。

> **G2 与 §6 第 1 条（写接口缺鉴权）是同一类错误的两面**：
> 「**前端隐藏入口不是安全边界**」与「**开发机上的文件不是项目资产**」——
> 前者的判断依据是"能不能被一个 HTTP 请求绕过"，后者是"换一台机器还在不在"。
> 两者都必须由**项目状态**（服务端策略 / 版本控制）强制，而不是靠人的自觉。

---

## 3. 待做实验与计划

> 本节是**全项目所有"还没做"的技术动作**的收拢处，按**主题**分组（不按它原先写在哪个文档里）。
> 每条给出：要做什么、为什么、前置条件、出处。
> **执行完一条就删掉它**，把结论写进对应的当前事实文档（用法与命令 → [05-运维与部署手册.md](./05-运维与部署手册.md)，原理 → `learn/`）。
>
> 「出处」列写的是**重构前**的文档编号（原 `09`~`14`），只用于追溯；
> 它们的后继是 `learn/02`（原 `11`）、`learn/03`（原 `12` + `13`）与 `archive/` 两篇。

### 3.1 性能与容量：基线已落地，SLO 已定稿

这一组对应 **G8**。**2026-09-14 完成了前半段**：造数据脚本、六个 k6 脚本、一键基线全部进仓库（`tools/load/`），
并在本地 compose 整栈上真跑出了 **B1 基线**（六接口各 10 RPS × 30 秒 + 冷缓存探针 + 14 条爬坡）。
**SLO 随之定稿**（下表）。剩下的是**容量**：它必须等目标机器规格确定才能有数字。

**前置条件**（原卡点已全部达成）

| # | 前置 | 状态 |
|---|---|---|
| 1 | CI 跑通 | ✅ |
| 2 | 部署形态（Dockerfile + compose） | ✅；**目标机器规格仍未定**——所以 SLO 第 8 条给的是**口径**而不是数字 |
| 3 | 造数据脚本：1,000 / 10,000 篇带长正文的文章 | ✅ `node tools/load/seed-posts.mjs [N]`（1,000 篇实测 1.63 s，幂等、可清理） |
| 4 | SLO 定稿（确认或修正下表） | ✅ 见下 |
| 5 | 压测环境关闭限流 | ✅ `docker compose -f docker-compose.yml -f tools/load/docker-compose.loadtest.yml up -d webapi`；⚠️ 之后必须 `docker compose restart nginx`，否则可能 502 |
| 6 | 冷缓存 / 热缓存两组步骤 | ✅ 热缓存走 k6 恒定载荷，冷缓存走 `cold-cache-probe.mjs`（每轮 `FLUSHDB` 后单次请求，20 轮） |

**已实测的本地基线（2026-09-14）**

> 数字逐格取自 [`tools/load/results/baseline-20260914-052631.md`](../tools/load/results/baseline-20260914-052631.md)，
> 未改写、未取整、未估算。**这张表是"实测值"，与下面那张 SLO 表的"目标值"不是一回事。**

环境规格：**20 核 WSL2 / 15 GiB（压测前空闲）｜全组件回环 `http://127.0.0.1:8080`（nginx → Kestrel）｜
PostgreSQL 18.6 + zhparser、Redis 7.4.11｜数据量 1,003 篇（已发布 796）｜k6 v1.4.1｜Release 容器镜像｜
限流已关闭**。

热缓存（稳态，各 10 RPS × 30 秒；写接口 0.5 RPS）：

| 接口 | 请求数 | p50 | p95 | p99 | max | HTTP / 业务失败 |
|---|---|---|---|---|---|---|
| `GET /api/posts` 首页列表 | 301 | **1.64 ms** | **2.21 ms** | **2.37 ms** | 2.48 ms | 0 / 0 |
| `GET /api/posts/{id}` 详情（含浏览量自增） | 301 | **4.06 ms** | **6.91 ms** | **8.35 ms** | 12.43 ms | 0 / 0 |
| `GET /api/posts/search` 中文全文检索 | 300 | **1.67 ms** | **2.41 ms** | **7.28 ms** | 17.33 ms | 0 / 0 |
| `GET /api/posts/archives` 归档 | 300 | **2.31 ms** | **3.25 ms** | **3.87 ms** | 3.99 ms | 0 / 0 |
| `GET /api/site/config` 站点配置 | 301 | **1.55 ms** | **2.05 ms** | **2.48 ms** | 3.19 ms | 0 / 0 |
| `POST /api/posts` 写文章（0.5 RPS） | 16 | 7.04 ms | 7.98 ms | 8.25 ms | 8.31 ms | 0 / 0 |

冷缓存（每轮 `FLUSHDB` 后单次请求，20 轮）：

| 接口 | p50 | p95 | p99 |
|---|---|---|---|
| `GET /api/posts/search` | **11.55 ms** | **13.09 ms** | 13.99 ms |
| `GET /api/posts/{id}` | 5.84 ms | 7.63 ms | 9.78 ms |
| `GET /api/posts/archives` | 6.05 ms | 7.48 ms | 7.75 ms |
| `GET /api/posts?page=1` | 4.41 ms | 5.77 ms | 6.31 ms |
| `GET /api/site/config` | 3.77 ms | 5.27 ms | 7.68 ms |

⚠️ **本机数字不能当容量结论。** 这轮是 **20 核 WSL2 回环 + 整栈同机**，**目标机器规格未定**；
爬坡只打了**一条全缓存端点**（`GET /api/posts`），数据库路径的容量没测。它的用途是
**契约判定与回归基线**（下次改动变慢了能不能看出来），**不是**容量承诺，也不是收紧 SLO 的理由。
复现命令见 `tools/load/README.md`。

**SLO（2026-09-14 定稿）**

| 接口 / 场景 | SLI | **目标值** | 本轮 |
|---|---|---|---|
| `GET /api/posts` 首页列表 | 服务端 p95 | < 300 ms | 草案原值 |
| `GET /api/posts/{id}` 文章详情 | 服务端 p95 | < 300 ms | 草案原值 |
| `GET /api/posts/{id}` 文章详情 | 服务端 p99 | ≤ 1 s ① | 🆕 ③ |
| `GET /api/posts/search` 全文检索 | 服务端 p95 | < 800 ms | 草案原值 |
| `GET /api/posts/search` 全文检索 | 服务端 p99 | ≤ 1 s ① | 🆕 ③ |
| `GET /api/site/config` 站点配置 | 服务端 p95 | < 300 ms | 🆕 ① |
| `GET /api/posts/archives` 归档 | 服务端 p95 | < 300 ms | 🆕 ① |
| `GET /api/posts/archives` 归档 | **响应体大小** | **≤ 256 KiB** | 🆕 ② |
| `GET /api/files/{name}` 文件 / 图片 | 服务端 p95 | < 200 ms | 草案原值（**本轮未覆盖**） |
| `POST /api/posts` 保存文章 | 服务端 p95 | < 500 ms | 草案原值 |
| `GET /health` 探针 | p99 | < 100 ms | 草案原值（**单独作为探针 SLI 维护**） |
| 全局错误率 | HTTP 5xx 比例 | < 0.1% | 草案原值 |
| 全局错误率 | 业务失败（`code !== 0`）比例 | < 0.1%；**429 单列** | 🆕 口径 |
| 单机吞吐 | 容量 | **在目标机器上，`GET /api/posts` 热缓存路径 p95 < 300 ms 时的最大稳定 RPS ≥ 峰值流量 × 2** | 🔧 ④ 改口径 |
| 可用性 | 月度 | 99.5%（约 3.6 小时 / 月） | 草案原值 |

> ⚠️ **实现口径比表里更严，这是刻意的。** `tools/load/k6/lib/common.js` 的阈值用的是 k6 的
> `http_req_failed`（含**全部 4xx**，不只是 5xx）。原因：统一响应体下"HTTP 200 也可能是失败"
> （见下面那条），而 4xx 大量出现同样是异常信号。
> 更关键的是 **429 必须单列并让整轮判定失败** —— 限流一开，`http_req_failed` 与
> `business_errors` 会同时被污染（限流响应体是 `{"code":4091,…}`，会被算成业务失败），
> 那时整轮数字全部作废。这个口径在 `common.js` 的注释里也写了一遍。

> ⚠️ **表里的值是"目标"，不是实测值。** 实测值见上面的基线表——两者相差数十倍是**正常的**，
> 因为 SLO 是**从用户体验倒推**的（首屏内容 1 秒内出现 ⇒ 后端留 300~500 ms；搜索是用户主动等待 ⇒ 放宽到 1 秒，
> 原理见 [learn/03](../learn/03-性能与运行时诊断.md) §6.2），**不是**从机器性能正推的。
> **"实测比目标快 50 倍"不是收紧目标的理由**：本机是 20 核回环，把 SLO 收到实测值附近，
> 等于把一个"用户可接受"的目标换成"这台开发机恰好达到"的目标，上线后立刻违反。
> 每条 SLO 都留有**错误预算**（p95 之下允许 1% 的请求超出）：没用完说明可以放心发版，用完了该停下来修稳定性。
> 每条阈值都能写进 k6 的 `thresholds` **自动判定**——判据不靠人读报告（样例见 `tools/load/k6/lib/common.js`）。

**为什么补这四条**（每条都由实测数据支撑，不补就是放着已知风险不管）

| # | 补充 | 为什么补（实测依据） |
|---|---|---|
| ① | `archives` 与 `site/config` **各补一条 p95 < 300 ms** | 两者都在**首屏路径**上，草案却漏了；实测热/冷 p95 分别为 3.25 / 7.48 ms 与 2.05 / 5.27 ms |
| ② | `archives` 补**响应体 SLI ≤ 256 KiB** | 实测单次响应 **103.4 KiB @ 796 篇已发布 ≈ 133 B/篇**，按线性外推 1 万篇 ≈ **1.3 MiB**。它是六接口里唯一"响应体随数据量线性增长"的（无分页 + 全量内存分组），**而延迟 SLO 完全看不见它**——服务端只有 3.25 ms，用户却在等 1.3 MiB 的传输 |
| ③ | **把 p99 写进 SLO**（详情与检索各一条 ≤ 1 s） | 只写 p95 会漏掉"重启 / 扩容 / TTL 集中到期"这类**最危险的时刻**。长尾就是缓存未命中：search 热 p99 **7.28 ms** ≈ **它自己的冷 p50 11.55 ms**（300 次请求里 5 个关键词各冷一次 ≈ 1.7%，正好落在 p99 位置） |
| ④ | 第 8 条"单机吞吐"**改成口径而不是数字** | 本机爬到 **2400 RPS 仍无拐点**（p95 1.82 ms、错误率 0%、webapi 只用了约 2.5 个核），瓶颈根本不在应用 ⇒ **任何基于本机的容量数字都是环境的数字**。所以改成"在目标机器上、热缓存路径 p95 < 300 ms 时的最大稳定 RPS ≥ 峰值 × 2"，目标机器规格一旦确定即可照着填 |

**两条口径修正**（不改任何数值）：

- **错误率要按业务口径统计**：统一响应体下 **HTTP 200 也可能是失败**，所以 `< 0.1%` 之外要加
  「`code !== 0` 比例 < 0.1%」，并且**把 429 单列**——429 是限流器在工作、不是应用故障，但对用户就是失败。
  本轮实测：六个接口合计 1,519 个请求，**HTTP 失败 0、业务失败 0、`dropped_iterations` 0、checks 100%**。
- **`GET /health` 的 p99 < 100 ms 保持**，但注意它是**依赖健康探测**（真实校验 PG + zhparser + 迁移 + Redis），
  放在业务流量下压会得到混合结论，建议**单独作为探针 SLI** 维护。

**一次性能测试要知道的四件事**

| 项 | 内容 |
|---|---|
| 压哪些接口（按优先级） | 首页列表 → 详情（会触发浏览量写入）→ 搜索（FTS + `ts_rank` + CTE）→ 归档（无分页 + 全量内存分组）→ 站点配置（5 次聚合）→ 写文章（低并发、高成本） |
| 分阶段执行 | **B0 冒烟 → B1 基准（六接口 + 冷缓存 + 爬坡）已完成（2026-09-14）** → L1 负载（按 SLO 峰值）→ S1 压力（找拐点）→ O1 优化（**只改一处**）→ L2 复测 → S2 浸泡（4~24 小时） |
| 完成判据 | ① 有基线数字且环境规格完整记录 ✅；② 每接口都有 p50 / p95 / p99 ✅；③ 与 SLO 逐条比对 ✅；④ 不达标项有定位结论 ✅（0 项不达标）；⑤ **脚本已进仓库** ✅ `tools/load/`；⑥ 至少跑过一次冷缓存 ✅ |
| 数据量 | 必须接近真实量级：数据太少时索引不生效、查询计划退化，测出来的数字会**给人虚假的安全感**。本轮按**最低可用量级 1,000 篇**建基线 |
| 工具与脚本 | 冒烟用 `bombardier`，正式压测用 k6（`thresholds` 里写 SLO、检查响应体的 `code === 0`、`BASE_URL` 可配置）。六个脚本 + 一键基线 + 造数据 + 冷缓存探针**已在 `tools/load/`**；怎么装、怎么跑、怎么关限流、13 条坑见 [`tools/load/README.md`](../tools/load/README.md) |

> 🧭 **一条比数字更重要的认知：五个读接口（列表 / 详情 / 检索 / 归档 / 站点配置）全部走 Redis 缓存**
> （`PostService` 的 `CacheKeys.PostList` / `PostDetail` / `PostArchives`、`SiteService` 的 `CacheKeys.SiteConfig`）。
> 所以**热缓存数字回答的是"稳态下的用户体感"，冷缓存数字才是数据库路径的真实成本**；
> 10 RPS 跑 30 秒时第一轮之后全是缓存命中，热数字里看不出"谁贵"。
> **只跑热缓存的 p95 会得出"检索和列表一样快"的错误结论**（热 2.41 vs 2.21 ms，几乎分不出差别），
> 而冷缓存把成本原样暴露出来：**检索 13.09 ms vs 列表 5.77 ms**。
> 更隐蔽的是**同一个接口内部**：search 热 p95 2.41 ms 看着很稳，**热 p99 却是 7.28 ms**——
> 长尾完全来自那几次缓存未命中（见下面第 ③ 条）。

> ⚠️ **k6 遇到含中文的路径会 panic**（实测：`unexpected k6 panic: stat /mnt/d/dotNET%E9%A1%B9%E7%9B%AE/...: no such file or directory`
> ——它把脚本路径当 URL 转义后再 `stat`）。仓库路径含中文，所以**手动 `k6 run tools/load/k6/xx.js` 会直接崩**；
> `run-baseline.sh` 靠 `/tmp` 下的 ASCII 软链规避（也可以改用 `docker run … grafana/k6`）。**这条必须记住，否则下次一定再踩。**

> ⚠️ **S2 浸泡测试排最后但不要跳过**：死元组堆积（实验 C）与内存泄漏**只有它能发现**。
> 跑一次 4 小时的浸泡成本很低，价值很高。

**另外两处热点**（读代码可确认，现象与原理见 [learn/03](../learn/03-性能与运行时诊断.md) §9）的观察点：

| 热点 | 观察点 |
|---|---|
| 归档接口把全部文章加载进内存后分组，且**不分页** | 归档接口的 p95 **随文章数增长的趋势**；若 10,000 篇时仍 < 300 ms，这条假设可以关闭（**响应体上限已单独进 SLO**，见上表第 ② 条） |
| 站点统计一次请求打 **5 个聚合查询**（含两个全表 `SUM`），结果被 Redis 缓存 | 清空 Redis 后 `GET /api/site/config` 的**首字节时间**（冷缓存下 5 个查询串行执行，是首屏最慢的一环）。本轮冷缓存探针已测：p50 3.77 ms / p95 5.27 ms，**在本机同时是最快的一环**——与"它是首屏最慢的一环"正好相反，值得在更大数据量下复核 |

**待做实验与计划（剩余）**

> 已完成的动作**从这里删除**，结论留在上面；下面只放**还没做**的，每条都给出前置条件。

| # | 待做 | 为什么 / 前置条件 | 出处 |
|---|---|---|---|
| 1 | **实验 C 的 S1 级复测** | P3 在 **10 RPS 下测出它不成立**（单篇热点 vs 100 篇分散：p95 5.99 vs 6.91 ms，无系统性恶化）。结论要**升级为"关闭"必须有 S1 级压力测试**（更高并发 + 观察 `pg_stat_activity.wait_event` 与 `n_dead_tup`）。**在 10 RPS 下不成立 ≠ 在高并发下不成立** | 实验 C（§3.2）、`results/raw/detail-same-post.txt` |
| 2 | **`GET /api/files/{name}` 与 `/health` 的独立 p99** | 本轮**未覆盖**：造数据没有造上传文件，该接口没有可用样本；`/health` 只在 k6 的 `setup()` 里做了一次门禁检查。前置：补一个"造 N 个媒体文件 + 元数据"的步骤、单独一条 10 RPS × 30s 的探针脚本 | 原文 §7 未覆盖清单 |
| 3 | **10,000 篇量级复测** | archives 的 **1.3 MiB 是线性外推**（由 796 篇 × 133 B/篇 推得），**未实测确认**；前置就一条命令 `node tools/load/seed-posts.mjs 10000`，重点看归档的**响应体与 p95 趋势** | P3 顾虑 ②、本表热点一 |
| 4 | **数据库路径的容量（不是延迟）** | 爬坡打的是全缓存端点；冷路径只测了单次延迟。做法：`--rate` 压"每次请求前清缓存"的场景，或直接压 PG | 原文 §7 未覆盖清单 |
| 5 | **S1 压力（找拐点）/ S2 浸泡 / L1 负载** | S1 是第 1、4、7 条的前置；**S2 排最后但不要跳过**——死元组与内存泄漏只有它能发现 | 原文分阶段执行 |
| 6 | **白盒：`dotnet-counters` / `trace` / `gcdump`** | 本轮只做黑盒基线；白盒对应 §3.2 的实验 A / B / D，与实验 C 的对照组配套做 | §3.2 |
| 7 | **容量 SLO 填数（SLO 第 8 条）** | 前置：**目标机器规格确定** + 在目标机器上跑一次爬坡。在那之前写任何数字都是猜的 | SLO 第 8 条 |

> ⚠️ **"假设不成立"也是有效结论，但要有边界。** 实验 C 在 10 RPS 下不成立、
> 归档接口在 1,000 篇量级下延迟与错误率都远低于 SLO，**这两条不是"没问题"，而是"在这个量级下没问题"**——
> 前者要 S1 级压力才能关闭，后者的风险根本不在延迟而在响应体（1 万篇外推 ≈ 1.3 MiB）；
> 站点配置冷缓存 p95 5.27 ms 是本机五条冷路径里**最快**的，与"它是首屏最慢的一环"的预期正好相反。
> 换一个量级它们都可能反转，这正是上表第 1、3 条存在的理由。

### 3.2 四个诊断实验：做透 2~3 个

**不要四个都做。** 每个实验的产出是"一个假设被证实或被推翻 + 一份可复现的脚本"，
四个各做一半不如两三个做透。推荐顺序：**A（成本最低）→ B（最贴内存分析）→ C 或 D**。

| 实验 | 要回答的问题 | 怎么做 / 看什么 | 出处 |
|---|---|---|---|
| **A. Server GC vs Workstation GC** | Server GC 是 Web SDK 给的**隐式默认值**（csproj 里一处都没写），多核上吞吐更好但内存占用显著更高；单核小内存 VPS 上可能反而更差 | 同一个压测脚本跑两组，唯一变量是 `DOTNET_gcServer`；看 RPS、p99、`gc-heap-size` 峰值、`working-set` 峰值、`% Time in GC`。产出：对比表 + **一行显式的 csproj 配置** | 原 `13 §8.1` |
| **B. 图片上传链路的内存峰值** | 4000 万像素图的像素缓冲约 160MB（推算），**它在托管还是非托管内存**——这决定后续所有分析方向 | 并发上传不同尺寸的图，同时监控 `loh-size`（应平稳）与 `working-set`（应飙升）。`loh-size` 平稳即证明像素在**非托管**，GC 调优无用；反之走 LOH 分析路径 | 原 `13 §8.2` |
| **C. 浏览量自增的行锁热点** | `IncrementViewCountAsync` 用 `ExecuteUpdateAsync` 对**同一行**高频 `UPDATE`：是否让热门文章的并发访问在这里串行化，并堆积死元组（行业里的"计数器列反模式"） | 压**同一篇**热门文章 vs 压 **100 篇不同**文章，比两组 p99；辅助看 `pg_stat_activity.wait_event`（`Lock:transactionid` / `Lock:tuple`）与 `n_dead_tup` 的增长曲线。**结论是"不成立"也要记录为已验证** | 原 `12 §9.2` 嫌疑二 ／ 原 `13 §8.3` |
| **D. 慢查询 TOP N 与索引优化** | 哪些查询真的慢、缺哪个索引 | 先做前置改造（见 §3.3「慢查询阈值可配」）→ 造真实数据量 → 压测 → 收集慢查询日志 → `EXPLAIN ANALYZE` → 优化 → 复测。重点怀疑 `GetArchivesAsync`、`GetStatsAsync`、全文检索的 CTE | 原 `13 §8.4` |

> 实验 C 在原始素材里被写过两遍（`12` 从"压测时该盯哪里"、`13` 从"诊断时该查什么"），
> **此处只登记一次**——它是同一个热点。

**实验 B 的候选修法**（都需先测再改，按预期收益排序）：

1. `DecoderOptions.TargetSize` 在**解码阶段**就缩到目标尺寸——收益最大，避免"先解 160MB 再缩小"；
2. 收紧 `MaxPixels`（如 4000 万 → 1200 万），直接砍掉最坏情况（代价：拒绝部分合法大图）；
3. 给 `MemoryAllocator` 设 `MaximumPoolSizeMegabytes`，限制池**保留**的内存；
4. **限制并发上传数**（信号量 / 限流规则），把峰值从 N 倍压到常数倍；
5. 去掉 `output.ToArray()` 的那份副本（需把下游签名改成 `ReadOnlyMemory<byte>`）。

> ⚠️ **查 API 必须查锁定版本的**：ImageSharp 官方文档站是最新版（v4），而本项目因许可锁 **3.1.12**。
> 已逐条核对本地包 XML：`TargetSize` / `MaxFrames` / `SkipMetadata` / `MaximumPoolSizeMegabytes` /
> `AllocationLimitMegabytes` / `Configuration.MemoryAllocator` / `MemoryDiagnostics` ✅ 可用；
> `ImageInfo.GetPixelMemorySize()` 与 `AccumulativeAllocationLimitMegabytes` ❌ 3.1.12 没有（v4 才有）。
> 像素内存是托管还是非托管**必须实测**，不能照搬文档结论。

### 3.3 工程化与交付：还没接上的东西

> **已落地**的门禁与其失败条件见 [learn/02-CI-CD实践.md](../learn/02-CI-CD实践.md) §4.5。
> 下表只列**还没做**的，以及它们的顺序与代价。

| # | 待接入 | 要做什么 / 为什么现在不做 | 出处 |
|---|---|---|---|
| 1 | **前端表单链路的自动化覆盖** | §1.1 的四项缺口（校验提示 / 保存后刷新 / 409 提示 / 骨架屏）目前**没有任何自动化检查**（浏览器级检查已移除），只能人眼在浏览器里验证；缺口明细与已暴露实例见 §1.1 | 原 `09 §1` |
| 2 | **前端 ESLint + Prettier** | 正确顺序：① 加配置 → ② 本地一次性格式化全库（一个巨大提交）→ ③ 再在 CI 开检查。反过来做，第一次流水线会冒出上千条告警，然后你会关掉它 | 原 `11 §9.5` 第 10 条 |
| 3 | **C# analyzer 门禁（G6 的后半）** | whitespace 门禁已上；analyzer 暂缓——实测 `dotnet format --severity info` 在本仓库报出一批建议（CA2016 / CA1873 / CA1816 / CA1861 / SYSLIB1045 / ASP0025 等），一次性纳入属过度工程化 | 原 `11 §9.3` |
| 4 | **数据库迁移可生成性校验** | CI 校验"模型快照与代码一致"，防"忘了提交迁移" | 原 `11 §5` 第 5 项 |
| 5 | **部署期密钥的平滑轮换** | 部署凭据（`DEPLOY_HOST` / `DEPLOY_USER` / `DEPLOY_SSH_KEY`）已改为 GitHub 的加密 Secret，服务器上的 `Jwt__SigningKey` 由 `deploy.sh init` 现生成 —— **剩下的是轮换**：需要在**一段时间内同时接受新旧密钥**。⚠️ 本项目只发 Access Token、无 Refresh Token，直接换密钥会让所有人被登出 | 原 `11 §8.5` |
| 6 | **迁移的多实例竞争** | 把启动时自动 `Migrate()` 拆成**独立的、只跑一次的部署步骤**（`DeploymentGuard` 只管缓存，不管迁移竞争） | 原 `11 §8.4` |
| 7 | **覆盖率阈值的后续** | 当前**刻意不设阈值**；等基线稳定后，只对"**新增代码**"考虑设阈值，而不是对总量 | 原 `11 §9.5` 第 8 条 |
| 8 | **依赖扫描升级为阻断** | 现在是 `continue-on-error`（只报告）。结果稳定后可升级；最省事的路径是给构建加 `-warnaserror:NU1901;NU1902;NU1903;NU1904` | 原 `11 §9.5` 第 7 条 |
| 9 | **慢查询阈值可配** | `SlowQueryInterceptor` 的 500ms 是代码里的常量；改成配置项（如 `Database:SlowQueryThresholdMs`），压测时降到 100ms 才能找到"温吞"查询（实验 D 的前置改造） | 原 `12 §9.2`、`§12` 坑 11 |

---

## 4. 明确"不做"的事项（不是技术债）

有意排除的范围，不是遗漏。**决策编号**（T / Q / E 系列）的完整理由见
[archive/决策记录.md](../archive/决策记录.md)。本节合并了原先分散在两处的两份清单，**去重后 13 条**。

| 事项 | 决定 | 理由 |
|---|---|---|
| SEO（站点地图、结构化数据、预渲染） | 不做（T10） | 纯 SPA 个人博客 |
| 自建评论系统 | 不做 | 外挂 giscus（GitHub Discussions），本站**不存评论数据** |
| Refresh Token | 不做（T7） | 只发 Access Token（30 分钟） |
| 作者自助注册 | 不做（T1） | 作者账号由管理员创建 |
| 细粒度 RBAC | 不做 | 只有 `Admin` / `Author` 两档角色 |
| 内容审核流 | 不做（Q6） | 多作者规模引入时再评估 |
| 国际化（i18n） | 不做（Q10） | 单语言站点 |
| UI 组件库（含 Naive UI） | 不做（E1 / Q13） | 自研 CSS 体系与 Aurora 视觉深度绑定，整体引入的冲突成本高于收益 |
| AutoMapper | 不做 | DTO 手写映射，显式且编译期可检查 |
| EF 延迟加载 | 不做 | 易产生 N+1，改为显式 `Include` / 投影 |
| 缓存命中率统计 | 不做（E7） | 收益低，增加运行时开销 |
| OSS / CDN 对象存储 | 本期不做（Q9） | 保留扩展点，将来迁移 |
| **备份功能** | **不做（已移除）** | 个人项目，不承担 RPO 承诺。备份脚本、定时任务、恢复演练**都已删除**；⚠️ 代价是破坏性迁移（删列 / 改类型）没有任何兜底，见 [05 §8.9](./05-运维与部署手册.md) |

> 一个项目的边界不只由"做了什么"定义，也由"明确不做什么"定义。

---

## 5. 已废弃方案（记录以免重提）

合并原先分散在三处的清单，**去重后 11 条**。归档时点的原始记录见
[archive/决策记录.md](../archive/决策记录.md) §5。

| 已废弃的方案 | 为什么废弃 | 被什么取代 |
|---|---|---|
| 乐观锁用 `byte[] RowVersion` + `[Timestamp]` | 依赖 SQL Server 的 `rowversion` 语义，**跨库不通用** | `int Version` + SQL 条件手动控制（[02-架构与数据模型.md](./02-架构与数据模型.md) §7.1） |
| 双登录入口（`/login` + `/admin/login`、两个登录端点） | `Author` 是署名实体、无法登录；双页面只造成命名歧义，**不提供任何安全性** | 单一入口 `/login` + `POST /api/auth/login`（T16） |
| 静态文件中间件暴露 `/media/**` | 无法做路径穿越防护，缓存头无法按分支控制 | 独立文件接口 `/api/files/**`（Q4，[02-架构与数据模型.md](./02-架构与数据模型.md) §13） |
| 用 `pg_trgm` 模糊匹配做搜索 | 无法满足中文检索需求 | 真全文检索 + zhparser（T9，[02-架构与数据模型.md](./02-架构与数据模型.md) §9） |
| 前端 `/admin/profile` 独立页 | 与「作者管理」职责重叠（都在维护 `Author`） | 重定向到 `/admin/authors`（T14b） |
| 静默重定向首页代替 404 | 产生**软 404**，搜索引擎与用户都判断不出 URL 不存在 | 真实的 404 页面 |
| `PagedResult.totalCount` 字段名 | 后端实际下发 `total`，前端读 `totalCount` → "共 N 篇"渲染成 `undefined` | 统一为 `total` |
| `[ResponseCache]` 标注文件下载 action | 会连"文件不存在"的 404 一起缓存一整天 | 命中后才在方法体内设置响应头（T18） |
| 用 `nohup … &` 启动后端 | 进程随 Bash 会话被清理，表现为前端突然全部 `ECONNREFUSED` | 用工具的后台任务机制持有进程 |
| 靠"前端隐藏按钮"保护写接口 | **前端隐藏入口不是安全边界**：直接发一个 HTTP 请求即可绕过 | 服务端授权策略**逐个端点**声明；该缺陷的关闭记录见 §6 第 1 条 |
| 自建 PostgreSQL 镜像（`deploy/postgres-zhparser.Dockerfile` 编译 SCWS + zhparser） | 社区已有活跃维护的多版本镜像，自建是重复劳动；且产物 439 MB，每次首装都要单独打包上传 | 社区镜像 `mixdeve/postgres-zhparser:18`（官方 postgres + zhparser，与 Redis 同类：**只拉不建**）。**注意**：该镜像自带脚本建出的 `chinese` 只映射 `n,v,a,i,e,l`，缺 `j,q` —— 由迁移 `20260918174440_EnsureChineseConfigTokenMappings` 补齐（[deploy/README.md](../deploy/README.md) §1） |

---

## 6. 已关闭的问题索引

2026-09-13 及之前修掉的问题，**过程不在本文重复**——现象 → 定位 → 根因 → 修法 → 验证 → 教训，
以及每条的**证据位置**（文件 / 提交 / 命令），都在
[archive/问题排查记录.md](../archive/问题排查记录.md)。下表与其文末的「已关闭的问题（索引）」
**一一对应，编号一致**，不要重排。

| # | 问题（一句话） | 处理 |
|---|---|---|
| 1 | `Categories` / `Tags` / `Site` 的写接口与文件上传没有 `[Authorize]`，匿名可调用（前端隐藏按钮把它掩盖了） | 2026-09-11 |
| 2 | 开发用 PG 容器是官方镜像 + 手工编译 zhparser，容器一重建就丢扩展与 `chinese` 配置 | 2026-09-11 |
| 3 | 搜索按发布时间倒序而非相关度（`ts_rank` 无法被 EF 翻译） | 2026-09-11 |
| 4 | 种子作者头像指向不存在的静态路由（恒 404） | 2026-09-12 |
| 5 | 依赖摆放不当：`@types/dompurify` 与 `sass` 放进了 `dependencies` | 2026-09-12 |
| 6 | 没有健康检查端点（只有不检查依赖的 `GET /`） | 2026-09-12 |
| 7 | 测试覆盖率低（初建时只有 1 个测试文件），测试数还长期写死在文档里 | 2026-09-12 建体系；2026-09-14 补度量 |
| 8 | 容器化与生产配置四个问题：限流**静默失效**、缺 `.dockerignore`、限流可被 `X-Forwarded-For` 绕过、nginx 上传上限小于应用上限 | 2026-09-12 |
| 9 | 用户可填任意媒体地址（外链 / 协议相对 / `javascript:` / `data:image/svg+xml`）；另有正文内联样式盖掉暗色主题、首屏背景图与文档不符、Logo 硬编码在 5 个文件里 | 2026-09-13 |
| 10 | 上传的 SVG 可构成存储型 XSS（同源内联下发 + 作者即可上传） | 2026-09-13 |
| 11 | 迁移链无法从零建库：生成列排在「创建 `chinese` 配置」之前，且三处绕过让 CI 永远全绿 | 2026-09-13 |

> 其中第 7、10、11 条后来升级为能力缺口（分别是 G7 覆盖率度量、G11 上传 SVG、G12 从零建库），
> 当前状态见 §2；其余条目不属于 G 清单。
