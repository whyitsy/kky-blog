# 00 · 文档规范

> **用途**：定义本项目的文档分区、写作规则、维护纪律，以及**全项目代码索引总表**。
> **读者**：项目作者本人（独立开发者），以及未来接手项目的开发者。
> **适用范围**：`docs/`、`learn/`、`archive/` 三区（`deploy/README.md`、`tools/*/README.md` 风格向本规范靠拢）。
> **最后核对**：2026-09-14 ｜ **代码依据**：仓库 HEAD

---

## 1. 两条铁律

独立开发者的文档最常见的两种死法：**变成"迭代流水账"**、**与代码脱节**（读者照着做必然踩坑，于是彻底不再信任文档）。

> **铁律一：文档描述"当前快照"，不是"变更历史"。** 变更历史属于 Git commit message。
> **铁律二：代码是唯一事实来源（Single Source of Truth）。** 文档是代码的**解释与索引**；
> 一旦发现冲突，以代码为准并**当场回改文档**，不允许留"文档可能有点旧"——那等于宣布文档作废。

---

## 2. 按用途分成三区（硬规则一）

铁律一、二对所有文档都成立，但"为什么"与"当时"必须另有去处，否则手册迟早被历史撑爆。
三区各干一件事，**放错区就是未来的矛盾源**：

| 区 | 唯一用途 | 允许写 | **禁止写** |
|---|---|---|---|
| **`docs/`** 参考手册 | 描述**当前代码事实** | 端点、表结构、配置键、命令、部署步骤、未决项 | ❌ "计划中 / 打算做"（属 [06-技术债与待办.md](./06-技术债与待办.md)）<br>❌ "已修复历史 / 曾经的问题"（属 `archive/`） |
| **`learn/`** 教材 | 讲**原理与为什么** | 通用原理、取舍论证、学习路线、动手任务 | ❌ 声称自己是事实来源<br>❌ 复述 `docs/` 的实现细节（两处维护 ⇒ 迟早矛盾） |
| **`archive/`** 留档 | 保存**过程与历史** | 决策当时的理由、已废弃方案、踩坑的完整定位过程 | ❌ **只增不改**：可补索引与日期，不许回头改写结论 |

> ⚠️ **信任级别不同**：`docs/` 与代码不符即为**缺陷**；`learn/` 只保证原理正确，
> 字段名 / 端点 / 配置键**以代码与 `docs/` 为准**；`archive/` 描述的是**当时**的状态，别照抄里面的代码片段。

---

## 3. 不写死会变的数字（硬规则二）

**凡由数据或运行结果决定的数字，一律不写进文档。** 它们必然过期，而过期一次，整篇文档的可信度就掉一档。

| 点名反例（真实发生过） | 为什么错 | 改写为 |
|---|---|---|
| `195 项测试` / `81 项` | 加一个测试就错；同一仓库曾同时存在多个互相矛盾的值 | 删掉具体值，或"实测见 CI 作业摘要" |
| `94 条断言` | 随库里的数据变化 | 同上 |
| `13 篇文章` | 开发库实测只有 3 篇 | "远低于让查询规划器认真对待的量级" |
| 覆盖率百分比 | 随代码漂移；原作者自己都注明"不是逐位确定的" | 保留结论，删掉数字 |
| `12 项缺口` 这类清单计数 | 清单本身就在表格里，计数必然与表格脱节 | 只留表格，不再说"有几条" |

> **例外（可以写）**：**配置值与规格**——`MaxPixels`、`MaxFileSize`、镜像 tag、外部工具版本、
> 行业常量（如 LOH 的 85,000 字节）。判定方法：**它由代码 / 配置写死，还是由数据 / 运行决定？**

---

## 4. 写作规则

### 4.1 状态标记

| 标记 | 含义 | 判定标准 |
|---|---|---|
| `[已实现]` | 代码已落地且验证过 | 能在仓库里指出具体文件，且构建 / 调用通过 |
| `[已决定]` | 已拍板但还没写代码 | 有明确结论，只是尚未实施 |
| `[计划中]` | 有想法但未拍板 | 还需要决策，可能变化 |
| `[已废弃]` | 曾经存在、现已移除 | 保留说明是为了防止有人再提同样的方案 |

**默认是 `[已实现]`**：不写标记的内容，读者有权理解为"这是当前事实"；因此**只有非当前事实才需要标记**。
按 §2，`[已决定]` / `[计划中]` 只出现在 [06](./06-技术债与待办.md)，`[已废弃]` 只在同篇 §5 与 `archive/`。

### 4.2 面向"三个月后的自己"

设想读者完全忘了这个项目：**术语首次出现必须解释**（或链接到 [learn/01](../learn/01-后端知识地图.md) 对应小节）；
**不写"显然""简单来说""众所周知"**；**命令必须可复制粘贴**并标注在哪执行（仓库根 / `Blog.Backend/` / Windows 侧），
不写"进入目录后执行"这种含糊指代。正文**不写绝对路径**（仓库目录名含非 ASCII 字符，各机器盘符也不同），
一律用仓库内相对路径。**对比、清单、参数说明一律用表格**——表格比段落容易核对，这是本规范最看重的性质。

### 4.3 明确写出"坑"，且同一件事只讲一遍

踩过的坑是个人项目最宝贵的资产，统一用 **⚠️** 标记并写清三要素：
**现象**（报错信息，**原样抄报错**便于将来搜索）· **根因** · **解法**。
需要解释"为什么"时**只在一个文件里详细论证**，其他文件用链接指过去——同一段论证抄两遍，就是在制造下一个矛盾。
（例：乐观锁的完整论证在 [02](./02-架构与数据模型.md) §7.1，[learn/01](../learn/01-后端知识地图.md) 只讲通用原理；
交付类命令统一放 [05-运维与部署手册.md](./05-运维与部署手册.md)，别处只链接。）

---

## 5. 每篇文档的唯一职责

职责重叠就会互相矛盾。写之前先问："这句话属于哪个文件？"

| 文件 | 唯一职责 | 不写什么 |
|---|---|---|
| [README.md](./README.md) | 三区入口、阅读路线、仓库结构 | 具体技术细节 |
| [00-文档规范.md](./00-文档规范.md) | 写作与维护规则 + 代码索引总表（本文件） | 项目内容本身 |
| [01-快速开始.md](./01-快速开始.md) | 从零跑起来：环境、账号、种子数据、配置速查 | 架构设计与原理 |
| [02-架构与数据模型.md](./02-架构与数据模型.md) | 分层、实体与表结构、跨层关键机制 | 端点契约、命令、通用原理教学 |
| [03-API参考.md](./03-API参考.md) | 端点清单：路径、方法、权限、参数、响应、错误码 | 实现原理、表结构 |
| [04-前端设计.md](./04-前端设计.md) | 前端路由、状态、组件、样式、构建 | 后端内容、通用原理教学 |
| [05-运维与部署手册.md](./05-运维与部署手册.md) | **所有命令的唯一去处**：启停、构建、测试、迁移、排错、部署 | 架构决策论证 |
| [06-技术债与待办.md](./06-技术债与待办.md) | **唯一允许"过期"**：未决项、能力缺口、待做、不做、已废弃 | 已修复问题的过程（属 `archive/`） |
| [learn/01](../learn/01-后端知识地图.md) | 后端知识地图：原理 + 指向本项目代码 | 事实性细节（以 `docs/` 为准） |
| [learn/02](../learn/02-CI-CD实践.md) | CI/CD 原理与本项目流水线设计 | 流水线的当前配置（看 `.github/workflows/ci.yml`） |
| [learn/03](../learn/03-性能与运行时诊断.md) | 性能度量与 .NET 运行时诊断的原理 | 测试结果与基线数字 |
| [archive/决策记录.md](../archive/决策记录.md) | T / Q / E 系列决策**当时的理由**、不做、废弃 | 当前实现状态 |
| [archive/问题排查记录.md](../archive/问题排查记录.md) | 踩过的坑及其**完整定位过程** | 还没解决的问题（属 [06](./06-技术债与待办.md)） |

**每篇必须有元信息块**（`> **用途**：… ｜ **读者**：… ｜ **最后核对**：YYYY-MM-DD ｜ **代码依据**：仓库 HEAD`），
"最后核对"是**文档可信度的凭证**——看到半年前的日期，读者就知道该怀疑它了，每次核对完必须更新。
**标题最多到三级**（`###`）；更细的内容用列表或表格，不要用四级标题——那说明该拆文件了。

---

## 6. 维护纪律

### 6.1 何时必须更新文档

| 触发场景 | 必须更新的文档 |
|---|---|
| 新增 / 删除 / 改动端点 | [03](./03-API参考.md) |
| 实体、表结构、索引、迁移；字段长度 / 校验规则 | [02](./02-架构与数据模型.md)（校验规则另需同步 `FieldLimits.cs`） |
| 新增页面或路由 | [04](./04-前端设计.md) |
| 配置项新增 / 改名、账号与种子数据 | [01](./01-快速开始.md) |
| 环境 / 命令 / 部署流程变化 | [05](./05-运维与部署手册.md) |
| 新增或修改流水线、门禁 | [05](./05-运维与部署手册.md) §8 + [06](./06-技术债与待办.md) §2 + **给它配一道失效哨兵** |
| 定下 / 修改性能目标、做了一次压测、解决了一个未决项 | [06](./06-技术债与待办.md) §3（结论进 `learn/03`）或把条目**移出**清单 |
| 踩了新坑并定位出根因、做出或推翻一个决策 | `archive/`（当前口径同步到 [06](./06-技术债与待办.md) §4–§5） |

### 6.2 提交纪律

> ⚠️ **提交前必须 `git show --stat HEAD` 复核。** 本项目历史上多次发生"无关文件被误删 / 误加并混入提交"，
> 以及"工作区文件仅因行尾 CRLF/LF 差异被整体纳入提交"。

- 用 `git add -A` / `git add .` 前先 `git status --short` 看清范围，**优先按路径精确 `git add <path>`**。
- 改动前用 `git ls-files <path>` 确认文件是"已跟踪"还是"计划新增"，避免误判为删除。
- **行尾统一 LF**（仓库根有 `.gitattributes`）。检查：`git ls-files --eol | grep w/crlf`（正常应为空）。
- 未跟踪文件丢失**不可恢复**，建议高频提交。

### 6.3 文档的"最后核对"动作

改了代码之后：① 跑通构建 / 测试 → ② 打开受影响的文档，**逐条核对**表格里的路径、字段名、端点、配置键
→ ③ 更新"最后核对"日期 → ④ 文档与代码**在同一个提交里**落地。

**第 ② 步不能省**。文档出错最典型的方式不是"写得不对"，而是**"改代码时忘了同步某一张表"**。
命令见 [05-运维与部署手册.md](./05-运维与部署手册.md) §7。

---

## 7. 全项目代码索引总表

> **想知道 X 在哪实现，先在这里查。** 本表只回答"去哪个文件"；**它是什么语义、为什么这么写**在 `docs/0X §Y` 里。
> 各篇原有的"与代码的对应关系"节（`docs/01` §9、`docs/02` §18、`docs/03` §14、`docs/04` §9、`docs/05` §10）与 `learn/01` 的总表都已指向本表；**新增代码时回来补一行**。

**后端 · 入口与横切**（`Blog.Backend/`）

- 全部端点定义 → `Blog.WebApi/Controllers/*.cs`（`docs/03`）
- 管道装配、授权策略、模型校验响应工厂、CORS、Serilog、**JWT 密钥强制校验、启动时自动迁移** → `Blog.WebApi/Program.cs`（`docs/02` §11/§17、`docs/01` §3.3）
- 中间件（异常 → 响应、限流） → `Blog.WebApi/Middleware/*.cs`（`docs/02` §4/§12）
- 健康检查（依赖探针） → `Blog.WebApi/HealthChecks/HealthCheckSetup.cs`（`docs/02` §15）
- 当前登录用户解析、部署拓扑校验（危险配置启动即失败） → `Blog.Infrastructure/Security/{CurrentUserResolutionMiddleware,HttpCurrentUser,DeploymentGuard}.cs`（`docs/02` §10/§16）
- DI 注册与生命周期（含部署校验的调用点） → `Blog.WebApi/Program.cs`、`Blog.{Application,Infrastructure}/DependencyInjection.cs`（`docs/02` §17）

**后端 · 应用层与领域层**（`Blog.Backend/`）

- 用例编排（应用服务）、请求 / 响应 DTO → `Blog.Application/Services/{Auth,Author,Category,Collection,Post,Site,Tag,User}/*.cs`（`docs/02` §3、`docs/03`）
- 统一响应体 / 分页 / 错误码 → `Blog.Application/Common/{ApiResponse,PagedResult,ErrorCodes}.cs`（`docs/03` §1.1–§1.3）
- 字段长度上限与护栏、媒体地址白名单 → `Blog.Application/Common/{FieldLimits,MediaPath}.cs`（`docs/02` §5/§10.6）
- 实体定义（含行为方法）、公共字段、仓储接口 → `Blog.Domain/Entities/`（含 `Base/BaseEntity.cs`）、`Blog.Domain/IRepository/`（`docs/02` §5/§7.2）
- 配置选项类、缓存 Key 规范 → `Blog.Application/Interfaces/{*Options.cs,CacheKeys.cs}`（`docs/01` §6、`docs/02` §8.3）
- 配置项的实际取值 → `Blog.WebApi/appsettings*.json`、`docker-compose.yml`、`.env.example`（`docs/01` §6–§7）

**后端 · 数据访问**（`Blog.Backend/Blog.Infrastructure/`）

- DbContext（映射、索引、关系、查询过滤器、种子数据） → `Persistence/BlogDbContext.cs`（`docs/02` §6）
- 迁移与设计期工厂 → `Persistence/{Migrations/,BlogDbContextFactory.cs}`（`docs/01` §5.2）
- 仓储基类（乐观锁、软删除）、写侧实现、工作单元 → `Persistence/{Repositories/BaseRepository.cs,Repositories/*Repository.cs,UnitOfWork.cs}`（`docs/02` §7）
- 读侧查询仓储 → `Persistence/Repositories/{Post,Collection,Site,Taxonomy}QueryRepository.cs`（`docs/02` §7.2）
- 中文全文检索（`ts_rank` + CTE） → `Persistence/Repositories/PostQueryRepository.cs`（`docs/02` §9）
- 慢查询拦截器 / 浏览量自增 / 归档与统计热点 / `IDisposable` 审查对象 → `Persistence/Interceptors/SlowQueryInterceptor.cs`、`Repositories/{Post,Site}QueryRepository.cs`、`PostRepository.cs`、`Persistence/UnitOfWork.cs`、`Caching/RedisCacheService.cs`（`learn/03` §9/§13.4）
- 运行时配置（GC 工作模式的隐式默认值） → `Blog.WebApi/bin/<Config>/net10.0/Blog.WebApi.runtimeconfig.json`（构建产物）（`learn/03` §12.5）

**后端 · 缓存、限流、文件、安全**（`Blog.Backend/Blog.Infrastructure/`）

- 缓存三实现（Null / Memory / Redis）、限流令牌桶（Redis Lua + 内存） → `Caching/`、`RateLimiting/`（`docs/02` §8/§12）
- 文件存储与白名单、图片优化（→ WebP、`MaxPixels` 解压炸弹闸门） → `Files/LocalFileStorageService.cs`、`Images/ImageSharpOptimizer.cs`（`docs/02` §13）
- JWT 签发与校验 / 密码哈希 → `Security/{JwtTokenService,Pbkdf2PasswordHasher}.cs`（`docs/02` §10）

**前端**（`Blog.FrontEnd/`）

- 入口与接线、路由表与全局守卫、三套布局 → `src/main.ts`、`src/router/index.ts`、`src/layouts/{Default,Author,Admin}Layout.vue`（`docs/04` §2–§3）
- HTTP 封装、错误码常量、401 回调 → `src/api/http.ts`（`docs/04` §4）
- 按控制器分文件的 API 模块、与后端 DTO 对齐的类型 → `src/api/*.ts`、`src/types/index.ts`（`docs/03` §13–§14）
- 状态管理（Pinia） → `src/stores/{auth,app,site}.ts`（`docs/04` §5）
- 组件（管理端 / 通用 / 骨架屏）、Markdown 渲染与清洗（XSS 防护） → `src/components/{admin,common,skeleton}/`、`src/utils/{markdown,media}.ts`（`docs/04` §6）
- 设计令牌与全局样式 → `src/styles/{tokens,global}.css`（`docs/04` §7）
- 构建、代理、环境变量；设计参考稿（**不参与构建**） → `vite.config.ts`、`.env*`、`参考设计规范/`（`docs/04` §1.2–§1.3/§2）

**工程：流水线、测试与规范**（仓库根）

- 流水线定义（作业划分、缓存、门禁、防假绿）、依赖安全扫描 → `.github/workflows/ci.yml`、`.github/dependabot.yml`（`learn/02` §4–§5）
- 集成测试夹具（连接串解析、关限流）、测试工程配置 → `Blog.Backend/Blog.Tests/{Infrastructure/BlogApiFactory.cs,Blog.Tests.csproj,TestEntryPoint.cs}`（`learn/02` §4.2/§7.4）
- 集成测试（权限 / 草稿 / 乐观锁 / 统一响应体 / 字段长度）、单元测试 → `Blog.Tests/Integration/`、`Blog.Tests/Unit/`、`Blog.Tests/Images/`（`docs/03` §12）
- 覆盖率口径（排除迁移与生成代码）、C# 格式规则 → `Blog.Backend/coverage.runsettings`、`.editorconfig`（`learn/02` §4.3/§5.2–§5.3）
- 解决方案 / 工具版本 / 前端依赖清单；行尾与忽略规则 → `Blog.Backend/{Blog.Backend.slnx,dotnet-tools.json}`、`Blog.FrontEnd/package.json`、`.gitattributes`、`.gitignore`（`docs/05` §4/§7.2）

**部署产物**

- 后端镜像 / nginx 镜像与配置 / 整栈编排 → `deploy/{webapi,nginx}.Dockerfile`、`deploy/nginx.conf`、`docker-compose.yml`、`.env.example`、`.dockerignore`（`docs/05` §8）
- PG 中文分词 → 社区镜像 `mixdeve/postgres-zhparser:18`（写死在 `docker-compose.yml` 的 `pgsql.image`）+ 迁移 `20260918174440_EnsureChineseConfigTokenMappings`（`docs/01` §5.4、`deploy/README.md` §1~§2）
- 部署产物说明与镜像切换 → `deploy/README.md`（`docs/05` §8.7）

---

## 8. 本规范的自我约束与相关文件

本文件同样遵守以上全部规则：它是"当前规范"，不是"规范演进史"；若某条规则导致的负担大于收益，**直接改本文件**。
相关：[README.md](./README.md)（文档入口与阅读路线）· [01-快速开始.md](./01-快速开始.md) · [learn/README.md](../learn/README.md)（学习路线）
